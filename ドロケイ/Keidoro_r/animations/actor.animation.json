{
	"format_version" : "1.8.0",
	"animations" : {
		"animation.marker" : {
			"loop" : true,
			"bones": {
				"marker": {
					"scale": "2.5 + math.sin(query.anim_time * 400.0) * 0.25",
					"position": [ 
						0, 
						"40 + math.sin(query.anim_time * 300.0)", 
						0 
					],
					"rotation": [ 
						"query.rotation_to_camera(0)", 
						"query.rotation_to_camera(1) - query.body_y_rotation", 
						0.0 
					]
				}
			}
		}
	}
}
